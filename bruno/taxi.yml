opencollection: 1.0.0
info:
  name: taxi
config:
  proxy:
    inherit: true
    config:
      protocol: http
      hostname: ''
      port: ''
      auth:
        username: ''
        password: ''
      bypassProxy: ''
  environments:
    - name: taxi
      variables: []
items:
  - info:
      name: taxi ride request
      type: http
      seq: 1
    http:
      method: POST
      url: '{{radioUrl}}/taxi/ride/request'
      body:
        type: json
        data: |-
          {
            "userId": "user-123",
            "pickup": "Times Square",
            "dropoff": "JFK Airport"
          }
      auth: inherit
    runtime:
      variables:
        - name: radioUrl
          value: http://localhost:3000
    settings:
      encodeUrl: true
      timeout: 0
      followRedirects: true
      maxRedirects: 5
  - info:
      name: Taxi ride assign
      type: http
      seq: 2
    http:
      method: POST
      url: '{{radioUrl}}/taxi/ride/request'
      body:
        type: json
        data: |-
          {
            "userId": "user-123",
            "pickup": "Times Square",
            "dropoff": "JFK Airport"
          }
      auth: inherit
    runtime:
      variables:
        - name: radioUrl
          value: http://localhost:3000
    settings:
      encodeUrl: true
      timeout: 0
      followRedirects: true
      maxRedirects: 5
  - info:
      name: Taxi ride Complete
      type: http
      seq: 3
    http:
      method: POST
      url: '{{radioUrl}}/taxi/ride/assign'
      body:
        type: json
        data: |-
          {
            "rideId": "ride_12345",
            "driverId": "driver_67890",
            "eta": 8
          }
      auth: inherit
    runtime:
      variables:
        - name: radioUrl
          value: http://localhost:3000
    settings:
      encodeUrl: true
      timeout: 0
      followRedirects: true
      maxRedirects: 5
bundled: true
extensions:
  bruno:
    ignore:
      - node_modules
      - .git
    exportedAt: '2026-04-30T09:14:38.934Z'
    exportedUsing: Bruno
